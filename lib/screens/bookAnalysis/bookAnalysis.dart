import 'package:flutter/material.dart';

class BookAnalysis extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Kitap Analizi'),
      ),
      body: Container(
        child: Center(
          child: Text('Kitap Analiz Sayfası'),
        ),
      ),
    );
  }
}
